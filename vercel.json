{
	"version": 2,
	"builds": [
		{
			"src": "packages/cli/package.json",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/public/$1"
		}
	],
	"outputDirectory": "packages/cli/dist"
}
