{
	"version": 2,
	"builds": [
		{
			"src": "packages/cli/package.json",
			"use": "@vercel/node",
			"config": { "distDir": "dist" }
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/public/$1"
		}
	],
	"outputDirectory": "packages/cli/dist"
}
